
/*
    ===== Código de TypeScript =====
*/

let nombre: string = 'Strider';
let hp: number | string = 9;  // La variable puede ser tanto de tipo number como string, 
                              // no es super recomendable (solo si no queda otra)
let estaVivo: boolean = true;       

hp = 'FULL';

console.log('Hola Mundo!');

// Todo este codigo TypeScript será traducido automaticamente a Javascript para que lo entienda el navegador